session_name: vim
start_directory: '~'
windows:
  - window_name: indent_blankline
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/indent-blankline.nvim
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: neovim
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/neovim
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: cmp-rg
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/cmp-rg
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: cmp-under-comparator
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/cmp-under-comparator
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: headlines.nvim
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/headlines.nvim
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: lsp-format.nvim
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/lsp-format.nvim
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
  - window_name: virt-column.nvim
    focus: True
    layout: main-horizontal
    start_directory: ~/dev/virt-column.nvim
    panes:
      - focus: True
        shell_command:
          - tmux resize-pane -Z
          - v
      - cl
